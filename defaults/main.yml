---
# Enable/disable entire role
ntp_enabled: yes

# ISO standardized timezone, i.e. Europe/Berlin
ntp_timezone: ''

# List of NTP timeserver URLs
ntp_servers:
  - 0.europe.pool.ntp.org
  - 1.europe.pool.ntp.org
  - 2.europe.pool.ntp.org
  - 3.europe.pool.ntp.org
